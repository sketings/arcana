import { colorsEnum } from '../utils/colors.util.js';

export enum logLevelsEnum {
  LOG = colorsEnum.GREEN,
  ERROR = colorsEnum.RED,
  WARN = colorsEnum.YELLOW,
  DEBUG = colorsEnum.PURPLE,
  VERBOSE = colorsEnum.CYAN
}
